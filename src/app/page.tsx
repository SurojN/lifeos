import Link from "next/link";
import { ArrowRight, CalendarDays, FileHeart, Landmark, Map, Sparkles } from "lucide-react";
import { BrandMark, LifeOrbit } from "@/components/brand-mark";
import { auth } from "@clerk/nextjs/server";

const areas = [
  { icon: FileHeart, label: "Health & records" },
  { icon: Landmark, label: "Money & goals" },
  { icon: CalendarDays, label: "Plans & appointments" },
  { icon: Map, label: "Travel & memories" },
];

export default async function LandingPage() {
  const { isAuthenticated } = await auth();
  const primaryHref = isAuthenticated ? "/dashboard" : "/sign-up";
  const primaryLabel = isAuthenticated ? "Open LifeOS" : "Get started";
  return <main className="relative min-h-screen overflow-hidden"><div className="ambient-grid absolute inset-0 -z-10"/><header className="mx-auto flex max-w-7xl items-center justify-between px-5 py-6 sm:px-8"><Link href="/" className="flex items-center gap-3 text-sm font-semibold tracking-tight"><BrandMark/>LifeOS</Link><nav className="flex items-center gap-2">{isAuthenticated ? <span className="hidden text-sm text-muted-foreground sm:inline">Welcome back</span> : <Link href="/sign-in" className="rounded-full px-4 py-2 text-sm text-muted-foreground transition hover:bg-white/70 hover:text-foreground">Sign in</Link>}<Link href={primaryHref} className="rounded-full bg-ink px-5 py-2.5 text-sm font-medium text-white shadow-lg shadow-emerald-950/10 transition hover:-translate-y-0.5">{primaryLabel}</Link></nav></header><section className="mx-auto grid min-h-[calc(100vh-88px)] max-w-7xl items-center gap-12 px-5 pb-16 pt-8 sm:px-8 lg:grid-cols-[1.05fr_0.95fr] lg:pb-24"><div className="relative z-10"><p className="eyebrow"><Sparkles className="size-3.5"/>PRIVATE PERSONAL OPERATING SYSTEM</p><h1 className="mt-6 max-w-3xl text-5xl font-medium leading-[0.98] tracking-[-0.055em] sm:text-6xl lg:text-7xl">Life gets scattered.<br/><span className="text-primary">Bring it together.</span></h1><p className="mt-7 max-w-xl text-lg leading-8 text-muted-foreground">Health records, money plans, appointments, trips, goals, and memories—organized calmly, with every important fact connected to its source.</p><div className="mt-9 flex flex-wrap items-center gap-3"><Link href={primaryHref} className="inline-flex h-12 items-center gap-2 rounded-full bg-primary px-6 text-sm font-semibold text-primary-foreground shadow-xl shadow-emerald-900/15 transition hover:-translate-y-0.5">{isAuthenticated ? "Continue to your workspace" : "Start your LifeOS"} <ArrowRight className="size-4"/></Link>{isAuthenticated ? <Link href="/settings/privacy" className="inline-flex h-12 items-center rounded-full border bg-white/60 px-6 text-sm font-medium backdrop-blur transition hover:bg-white">Privacy controls</Link> : <Link href="/sign-in" className="inline-flex h-12 items-center rounded-full border bg-white/60 px-6 text-sm font-medium backdrop-blur transition hover:bg-white">I have an account</Link>}</div><div className="mt-12 grid max-w-2xl grid-cols-2 gap-x-5 gap-y-4 sm:grid-cols-4">{areas.map(({ icon: Icon, label }) => <div key={label} className="flex items-center gap-2 text-xs font-medium text-muted-foreground"><span className="grid size-8 shrink-0 place-items-center rounded-xl bg-white shadow-sm"><Icon className="size-4 text-primary"/></span>{label}</div>)}</div></div><div className="relative grid min-h-[430px] place-items-center lg:min-h-[600px]"><LifeOrbit/><div className="float-card absolute left-0 top-[18%] w-48 sm:left-[4%]"><span className="status-dot"/>Next appointment<p className="mt-2 text-base font-semibold text-foreground">Dental check-up</p><p className="mt-1 text-xs text-muted-foreground">Friday · 10:30 AM</p></div><div className="float-card absolute bottom-[16%] right-0 w-52 sm:right-[2%]"><span className="text-xs font-medium text-muted-foreground">Retirement goal</span><p className="mt-2 text-2xl font-semibold tracking-tight text-foreground">68% on track</p><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-muted"><span className="block h-full w-[68%] rounded-full bg-primary"/></div></div></div></section></main>;
}
